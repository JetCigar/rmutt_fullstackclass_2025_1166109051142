# ---------------------------------------------------------
# DATABASE CONNECTION (สำหรับ Docker Compose)
# ---------------------------------------------------------
# รูปแบบ: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
# ตอนรันใน Docker: HOST ให้ใส่ชื่อ service ของฐานข้อมูล (คือ postgres)

DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_NAME=ecommerce
DATABASE_URL="postgresql://postgres:your_secure_password_here@postgres:5432/ecommerce?schema=public"